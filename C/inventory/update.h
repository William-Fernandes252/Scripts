#ifndef UPDATE_H
#define UPDATE_H

/*
    update:     Changes the quantity on hand of the 
                item corresponding to the given number.
                Prints an error message if the item 
                does not exist in the database.
*/
void update();

#endif